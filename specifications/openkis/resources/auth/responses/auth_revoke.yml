description: Access Token 폐기 성공

content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: string
          description: HTTP 응답코드
          example: 200
        message:
          type: string
          description: 응답메시지
          example: 접근토큰 폐기에 성공하였습니다
