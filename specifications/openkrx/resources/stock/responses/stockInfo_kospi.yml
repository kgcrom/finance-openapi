description: 유가증권 종목 기본정보 조회 성공

content:
  application/json:
    schema:
      type: object
      properties:
        OutBlock_1:
          type: array
          items:
            $ref: ../models/stockInfo_response.yml
