operationId: getRegularKeyAccountAuditorNonAuditServiceConclusionStatus

summary: 회계감사인과의 비감사용역 계약체결 현황

description: 정기보고서(사업, 분기, 반기보고서) 내에 회계감사인과의 비감사용역 계약체결 현황을 제공합니다.

tags:
  - RegularDisclosureKey

parameters:
  - $ref: ../../shared/parameters.yml#/corp_code
  - $ref: ../../shared/parameters.yml#/bsns_year
  - $ref: ../../shared/parameters.yml#/reprt_code

responses:
  "200":
    $ref: responses/account_auditor_non_audit_service_conclusion_status.yml
