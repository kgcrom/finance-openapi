operationId: getRegularKeyDirectorAuditAllMendingStatusMendingPaymentAmountTypeClassification

summary: 이사·감사 전체의 보수현황(보수지급금액 - 유형별)

description: 정기보고서(사업, 분기, 반기보고서) 내에 이사·감사 전체의 보수현황(보수지급금액 - 유형별)을 제공합니다.

tags:
  - RegularDisclosureKey

parameters:
  - $ref: ../../shared/parameters.yml#/corp_code
  - $ref: ../../shared/parameters.yml#/bsns_year
  - $ref: ../../shared/parameters.yml#/reprt_code

responses:
  "200":
    $ref: responses/director_audit_all_mending_status_mending_payment_amount_type_classification.yml
